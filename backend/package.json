{
  "name": "CartIQ-mobile-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone search API for CartIQ_mobile — independent of CartIQ_web.",
  "engines": {
    "node": ">=22.9.0"
  },
  "scripts": {
    "dev": "node --env-file-if-exists=.env --experimental-strip-types --watch index.ts",
    "start": "node --env-file-if-exists=.env --experimental-strip-types index.ts",
    "postinstall": "playwright install --with-deps chromium",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/services/*.test.ts src/services/locators/*.test.ts src/services/browser/*.test.ts src/services/browser/stores/*.test.ts src/routes/*.test.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "playwright": "^1.61.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22",
    "typescript": "^5"
  }
}
