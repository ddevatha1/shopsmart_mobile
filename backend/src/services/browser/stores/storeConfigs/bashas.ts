/** Bashas' — reconnaissance passed (HTTP 200). A "captcha" string is
 * present in a form-validation error-message dictionary, not a
 * page-blocking challenge. */
import type { StoreOnboardingConfig } from './types.ts';

export const bashas: StoreOnboardingConfig = {
  storeName: "Bashas'",
  homepage: 'https://www.bashas.com/',
};
