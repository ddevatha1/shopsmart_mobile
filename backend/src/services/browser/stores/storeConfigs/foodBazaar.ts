/** Food Bazaar — reconnaissance passed (HTTP 200, no challenge signature),
 * not yet run through the full discovery pipeline. */
import type { StoreOnboardingConfig } from './types.ts';

export const foodBazaar: StoreOnboardingConfig = {
  storeName: 'Food Bazaar',
  homepage: 'https://www.foodbazaar.com/',
};
