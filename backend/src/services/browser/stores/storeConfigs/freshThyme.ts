/** Fresh Thyme — reconnaissance passed (redirects cleanly to HTTP 200). */
import type { StoreOnboardingConfig } from './types.ts';

export const freshThyme: StoreOnboardingConfig = {
  storeName: 'Fresh Thyme',
  homepage: 'https://www.freshthyme.com/',
};
