/** Lidl (US) — reconnaissance passed (HTTP 200, no challenge signature). */
import type { StoreOnboardingConfig } from './types.ts';

export const lidl: StoreOnboardingConfig = {
  storeName: 'Lidl',
  homepage: 'https://www.lidl.com/',
};
