/** Natural Grocers — reconnaissance passed (HTTP 200, no challenge
 * signature). */
import type { StoreOnboardingConfig } from './types.ts';

export const naturalGrocers: StoreOnboardingConfig = {
  storeName: 'Natural Grocers',
  homepage: 'https://www.naturalgrocers.com/',
};
