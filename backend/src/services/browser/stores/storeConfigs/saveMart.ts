/** Save Mart — reconnaissance passed (redirects cleanly to HTTP 200, no
 * challenge signature). */
import type { StoreOnboardingConfig } from './types.ts';

export const saveMart: StoreOnboardingConfig = {
  storeName: 'Save Mart',
  homepage: 'https://www.savemart.com/',
};
